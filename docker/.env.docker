# Docker Compose variable substitution
# This file is ONLY used by docker-compose for ${VAR} substitution.
# It does NOT affect local dotnet run (which uses ../.env and appsettings.json).

POSTGRES_DB=iti_erp
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Agent#007
JwtSettings__Secret=sjrfhw2234hskeiflscnw82op_urjf23
SeedAdmin__Password=Admin@123
